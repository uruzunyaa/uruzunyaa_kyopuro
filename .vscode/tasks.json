{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "run (active file, ACL)",
      "type": "shell",
      "command": "bash",
      "args": [
        "-lc",
        "set -e; ROOT=\"${workspaceFolder}\"; mkdir -p \"$ROOT/.bin\"; OUT=\"$ROOT/.bin/${fileBasenameNoExtension}\"; g++ -std=gnu++20 -O2 -Wall -Wextra -pedantic -I \"$ROOT/ac-library\" -o \"$OUT\" \"${file}\"; \"$OUT\""
      ],
      "options": { "cwd": "${workspaceFolder}" },
      "problemMatcher": ["$gcc"]
    }
  ]
}
